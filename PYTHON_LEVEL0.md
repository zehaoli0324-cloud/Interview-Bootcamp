# PYTHON LEVEL 0 — 从“看懂题目”到能独立写 Easy

> 这一层不是刷 LeetCode。目标是让你能读懂题面、变量、下标、循环和函数，并把一个简单思路翻译成 Python。
>
> **退出 Level 0 后就停止做这些微型练习。** 不用追求“Python 大全”。

## Level 0 的退出标准

满足以下 6 条即可进入真正算法题：

1. 看见 `nums = [5, 8, 2]` 能解释 `nums[0] / nums[1] / nums[2]`；
2. 能写一个 `for` 循环遍历列表；
3. 能写 `if` 判断；
4. 能写一个接收参数并 `return` 的函数；
5. 能用 `dict` 保存 `key -> value`；
6. 能独立完成 Z15–Z18 中至少 3 题。

---

# Z0 — 先学会读代码

## Z1 下标

```python
nums = [5, 8, 2]
```

回答：
- `nums[0]` 是什么？
- `nums[2]` 是什么？
- 为什么没有 `nums[3]`？

## Z2 变量

```python
x = 5
y = x + 2
```

回答 `x`、`y` 分别是什么。

## Z3 布尔判断

判断以下表达式是真还是假：

```python
5 > 3
2 == 4
7 != 8
```

## Z4 函数调用

```python
def add(a, b):
    return a + b
```

回答 `add(2, 5)` 得到什么，以及 `return` 的作用。

---

# Z1 — 列表 + 循环

## Z5 打印每个元素

输入：

```python
nums = [3, 1, 4]
```

要求依次打印三个数字。

## Z6 求和

写函数：

```python
def sum_numbers(nums):
    ...
```

例如 `[3, 1, 4] -> 8`。

## Z7 求最大值

不使用 `max()`，遍历列表找最大值。

例如 `[3, 9, 4] -> 9`。

## Z8 统计偶数

例如 `[1, 2, 4, 5] -> 2`。

## Z9 找第一次出现的位置

例如：

```python
nums = [7, 3, 9, 3]
target = 3
```

返回 `1`。

---

# Z2 — if + 状态

## Z10 统计大于阈值的数

```python
nums = [2, 7, 5, 10]
threshold = 5
```

返回 `2`。

## Z11 判断是否存在目标值

`[2, 7, 5], target=7 -> True`

## Z12 去重但不要求保序

先允许使用 `set`，理解“去重”是什么意思。

## Z13 计数器

统计字符串中字母 `a` 出现几次。

`"banana" -> 3`

## Z14 两层循环理解

给 `[4, 7, 1]`，列出所有不同位置的二元组合：

```text
(0,1), (0,2), (1,2)
```

这一步为 Two Sum 的暴力解做准备。

---

# Z3 — 函数 + dict

## Z15 频次统计

输入：

```python
items = ["A", "B", "A", "C", "A"]
```

返回：

```python
{"A": 3, "B": 1, "C": 1}
```

要求自己用 `dict` 实现，不先用 `Counter`。

## Z16 记录最后位置

输入：

```python
items = ["A", "B", "A", "C"]
```

返回每个元素最后出现的下标：

```python
{"A": 2, "B": 1, "C": 3}
```

## Z17 找补数：只做思维，不直接写 Two Sum

给：

```python
x = 4
target = 10
```

问：另一个数需要是多少？

再问：如果当前值是 `7`、`1`、`9` 呢？

核心关系：

```text
need = target - x
```

## Z18 Two Sum 暴力版

现在才回到：

```python
def two_sum(nums, target):
    ...
```

先允许 O(n²) 两层循环。

目标不是最优，而是确保：
- 看懂输入；
- 会访问 `nums[i]`；
- 会比较 `nums[i] + nums[j] == target`；
- 会 `return [i, j]`。

---

# Z4 — 从暴力到优化

## Z19 为什么 O(n²) 会慢

不要背定义。用 `n=4 / 100 / 100000` 想象需要比较多少对。

理解：两层遍历的工作量会随 n² 增长。

## Z20 Two Sum HashMap 版

只有 Z18 独立完成后才教。

每看到一个数 `x`：

1. 计算 `need = target - x`；
2. 查询以前有没有见过 `need`；
3. 有则返回两个下标；
4. 没有则记录当前 `x -> index`。

要求能回答：

> 字典里保存的是什么？为什么先查再存？

---

# Tutor 教法

如果候选人在 Level 0：

- 一次只教一个语法/概念；
- 每解释一个概念立刻让候选人回答一个 30 秒小题；
- 不说“这个很简单”；
- 不连续输出一整章教程；
- 候选人能自己答就立即减少提示；
- Z15–Z18 通过 3/4 后，直接退出 Level 0。

## 禁止

- Level 0 连刷几十道语法题；
- 背 Python 冷门语法；
- class / decorator / metaclass；
- 复杂面向对象设计；
- 还没懂 `for` 就讲 sliding window。

**Level 0 是跳板，不是目的地。**