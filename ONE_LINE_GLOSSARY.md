# ONE LINE GLOSSARY — 一句话扫盲手册

> 用法：第一次看到陌生词时，只看这一句，先建立“它大概是干什么的”的直觉；后面真正训练到这个知识点时再展开。
>
> **不要试图一次背完。** 这份文件是“地图”，不是考试内容。

---

# 0. 先学会看懂一道 Coding 题

- **Input / 输入**：题目交给你的一些数据，比如一串数字、一个字符串或一棵树。
- **Output / 输出**：题目希望你的程序最后返回或打印出来的答案。
- **Example / 样例**：题目用一个具体输入和正确输出示范“我要你干什么”。
- **Constraint / 约束**：告诉你输入可能有多大、有没有重复、能不能为空等限制条件。
- **Variable / 变量**：给一个值起名字，例如 `target = 10` 就是把数字 10 叫做 target。
- **List / 列表**：按顺序装很多值的容器，例如 `[5, 8, 2]`。
- **Index / 下标**：列表里每个元素的位置编号，Python 从 0 开始数。
- **`nums[i]`**：表示列表 `nums` 中第 `i` 个位置上的元素。
- **Function / 函数**：把一段可重复使用的操作封装成一个有名字的小工具。
- **Parameter / 参数**：调用函数时交给它处理的数据。
- **Return / 返回值**：函数做完以后把结果交回去。
- **`if` / 条件判断**：只有某个条件成立时才执行后面的代码。
- **`for` / 循环**：把一件事对一组元素一个一个重复执行。
- **`while` / 循环**：只要某个条件还成立，就继续重复执行。
- **Boolean / 布尔值**：只有 `True` 和 `False` 两种结果，表示“是/否”。
- **Trace / 手动走代码**：拿一个很小的输入，一行一行模拟变量怎么变化。
- **Edge case / 边界情况**：容易把代码弄坏的特殊输入，比如空数组、一个元素、重复值。
- **Bug / 程序错误**：代码能写出来但结果不对、会报错或会卡死的问题。
- **Syntax error / 语法错误**：Python 连代码格式都看不懂，程序还没开始运行就报错。
- **Runtime error / 运行时错误**：代码语法没问题，但跑到某一步才崩掉。
- **Correctness / 正确性**：代码是否对所有合法输入都能给出正确答案。
- **Time complexity / 时间复杂度**：输入变大时，程序运行时间大概增长得有多快。
- **Space complexity / 空间复杂度**：输入变大时，程序额外占用内存大概增长得有多快。
- **O(n)**：输入有 n 个元素时，大约只需要把数据看一遍。
- **O(n²)**：输入有 n 个元素时，常见含义是“两层循环把很多元素两两比较”。
- **Invariant / 不变量**：算法执行过程中始终必须保持为真的规则，是解释“为什么这段代码正确”的核心。
- **Brute force / 暴力解**：先用最直接的方法把所有可能情况试一遍，通常容易理解但速度不一定快。
- **Optimization / 优化**：在保证答案正确的前提下，让程序更快、更省内存或更稳。

---

# 1. Python 与最常见数据结构

- **String / 字符串**：一串文字或字符，例如 `"medical"`。
- **Tuple / 元组**：和列表类似但通常不修改，常用于把多个值组合成一个 key。
- **Dict / 字典**：保存“key → value”对应关系的结构，适合快速通过一个东西找到另一个东西。
- **HashMap / 哈希表**：本质上就是 Python `dict` 这种可以平均 O(1) 查找 key 的结构。
- **Set / 集合**：只保存“不重复的元素”，特别适合快速判断“这个东西见过没有”。
- **Stack / 栈**：像一摞盘子，最后放进去的东西最先拿出来。
- **Queue / 队列**：像排队，最先进去的东西最先出来。
- **Deque / 双端队列**：可以从头和尾都高效加入或删除元素的队列。
- **Linked List / 链表**：每个节点除了自己的值，还保存“下一个节点在哪里”。
- **Node / 节点**：链表、树或图中的一个基本元素。
- **Pointer / 指针（面试语境）**：通常指一个变量当前指向哪个节点，而不是要求你理解 C 语言内存地址。
- **Tree / 树**：一种有父子层级的数据结构，例如二叉树。
- **Binary Tree / 二叉树**：每个节点最多有两个孩子的树。
- **Graph / 图**：很多节点之间可以任意连接，比树更一般。
- **Grid / 网格**：二维矩阵形式的数据，例如医学图像像素或“岛屿题”的方格。
- **Heap / 堆**：能很快找到当前最小值或最大值的一种数据结构。
- **Min-heap / 最小堆**：堆顶永远是当前最小元素。
- **Top-K**：从很多元素里只找最大的或最重要的 K 个。
- **Counter**：Python 用来统计每个元素出现多少次的现成工具。
- **defaultdict**：Python 的字典变体，访问不存在的 key 时可以自动创建默认值。

---

# 2. 常见 LeetCode / 牛客题型

- **Two Sum 类型**：一边扫描数据，一边快速判断“和当前值配对的另一个值以前出现过没有”。
- **HashMap 题**：核心通常是用字典把“以前见过的信息”保存起来，避免重复搜索。
- **Stack 题**：核心通常是处理“最近一个还没解决的东西”，比如括号配对。
- **Sliding Window / 滑动窗口**：用左右两个边界维护一段连续区间，右边扩张、条件不满足时左边收缩。
- **Two Pointers / 双指针**：用两个位置变量一起移动，减少不必要的重复遍历。
- **Interval / 区间题**：处理 `[start, end]` 形式的数据，常见操作是排序、判断重叠和合并。
- **BFS / 广度优先搜索**：像水波一样一层一层向外扩展，通常用队列实现。
- **DFS / 深度优先搜索**：沿一条路尽可能走到底，再回来尝试其他路。
- **Visited / 已访问集合**：记录哪些节点已经处理过，避免图或网格里反复走同一个地方。
- **Binary Search / 二分查找**：每次排除一半搜索范围，从而快速在有规律的数据中找答案。
- **Binary Search on Answer / 对答案二分**：不是直接找某个数组元素，而是在“可能的答案范围”里不断猜中间值并判断可不可行。
- **Sorting / 排序题**：先把数据按某个规则排好，再利用顺序简化后续处理。
- **Quickselect**：类似快速排序的 partition 思想，只定位第 K 大而不把整个数组完全排好。
- **DP / Dynamic Programming / 动态规划**：把大问题拆成很多互相重用的小问题，并保存已经算过的结果。
- **State / DP 状态**：一个变量表示“当前已经解决到哪里、掌握了什么信息”。
- **Transition / 状态转移**：说明当前状态如何由前面的状态计算出来。
- **LRU Cache**：容量满时淘汰“最久没有使用”的数据，经典实现是 HashMap + 双向链表。
- **Sentinel node / 哨兵节点**：人为放一个假的头尾节点，让链表边界操作更统一、更少特殊判断。
- **Recursion / 递归**：函数调用自己，常用于树和 DFS，但递归太深可能导致栈溢出。

---

# 3. 文件、数据与工程基础

- **JSON**：一种常见文本数据格式，用 `{}` 和 `[]` 表示结构化信息。
- **JSONL**：每一行都是一条独立 JSON 记录，特别适合大规模模型评测数据。
- **Parsing / 解析**：把原始文本或文件转换成程序能操作的结构化数据。
- **Schema / 数据结构规范**：规定一条数据应该有哪些字段、每个字段是什么类型。
- **Malformed / 格式损坏**：一条数据不符合预期格式，程序不能正常解析。
- **Normalization / 规范化**：把不同写法统一成相同标准，例如大小写、单位、诊断名称。
- **Deduplication / 去重**：识别重复记录并只保留需要的一份。
- **Timestamp / 时间戳**：表示一条数据或事件发生的时间。
- **GroupBy / 分组**：按照某个字段把数据拆成多个组后分别统计。
- **Aggregation / 聚合**：把很多样本汇总成 count、平均值、通过率等统计结果。
- **Pipeline / 流水线**：数据按固定步骤从输入一路处理到最终输出的完整流程。
- **Batch / 批处理**：一次处理一批样本，而不是人工逐条处理。
- **Async / 异步**：等待网络或外部 API 时不让程序闲着，可以同时处理其他任务。
- **Concurrency / 并发**：同一时间让多个任务推进，提高吞吐量。
- **Bounded concurrency / 有界并发**：并发可以开多个，但限制最大数量，避免把服务打爆。
- **Timeout / 超时**：一个请求等待太久就主动终止，而不是永远卡住。
- **Retry / 重试**：遇到暂时性失败时重新执行任务。
- **Backoff / 退避**：每次重试之间逐渐等更久，避免服务已经拥堵还疯狂重试。
- **Jitter / 随机抖动**：在重试等待时间里加一点随机值，避免大量任务同时再次请求。
- **Idempotency / 幂等性**：同一个任务重复执行几次，最终效果仍然和执行一次一样。
- **Checkpoint / 检查点**：把已经完成到哪里保存下来，程序崩了以后不用从头开始。
- **Resume / 断点续跑**：读取检查点后从未完成的位置继续执行。
- **Run ID**：给一次完整评测运行分配的唯一编号，方便追踪这批结果来自哪一次运行。
- **Logging / 日志**：记录程序发生了什么，方便排查错误和追溯过程。
- **Observability / 可观测性**：通过日志、指标、状态等知道系统现在发生了什么和哪里出问题。
- **Provenance / 溯源**：能追查某条数据或分数来自哪个来源、哪个模型、哪个 prompt、哪个代码版本。
- **Versioning / 版本管理**：给模型、数据、prompt、judge、代码都标版本，避免结果无法复现。
- **Backend service / 后端服务**：在服务器端接收请求、执行逻辑并返回结果的程序。
- **Task scheduling / 任务调度**：决定大量任务什么时候执行、谁来执行、失败后怎么重新安排。

---

# 4. Track A：医疗大模型评测 / 数据工程

## 4.1 最基础的评测词汇

- **Evaluation / Eval / 评测**：用一套固定任务和指标判断模型到底好不好、哪里好、哪里差。
- **Benchmark / 基准测试**：一套固定且可重复使用的任务集合，用来比较不同模型或版本。
- **Sample / 样本**：评测集中的一条具体题目、输入或病例。
- **Prediction / 预测**：模型对一条样本给出的答案。
- **Gold / Ground Truth / 标准答案**：我们认为可信、用于判断模型对错的参考答案。
- **Independent truth / 独立真值**：标准答案不能由被测模型或同一个 judge 自己生成，否则容易循环论证。
- **Metric / 指标**：把模型表现压缩成数字的计算方法，例如准确率、F1。
- **Rubric / 评分标准**：把“一个好答案应该满足什么”拆成具体可判断的维度。
- **Task taxonomy / 任务分类体系**：把一个大任务拆成不同类型，例如诊断、用药、报告解读、工具调用。
- **Slice / 切片**：从整体数据里挑出某一类样本单独看，比如心血管、高风险或难题。
- **Error taxonomy / 错误分类体系**：把模型失败按照原因分组，而不是只说“答错了”。
- **Failure analysis / 失败分析**：研究模型为什么错、哪些场景集中出错以及应该怎么改。
- **Error attribution / 问题归因**：判断错误究竟来自知识不足、检索失败、推理错误、工具错误还是评测本身。
- **Regression set / 回归集**：长期固定保留的一组题，用来确认新版本没有把以前会的能力弄坏。
- **Regression evaluation / 回归评测**：新模型上线前重新跑固定题集，检查能力有没有退步。

## 4.2 分类指标

- **TP / True Positive**：应该判对的东西，模型也正确判对了。
- **FP / False Positive**：模型说有，但实际上没有，是“误报”。
- **FN / False Negative**：实际上有，但模型没发现，是“漏报”。
- **Precision / 精确率**：模型说“有”的那些结果里，有多少是真的。
- **Recall / 召回率**：所有真正应该找到的结果里，模型找到了多少。
- **F1**：把 Precision 和 Recall 综合成一个数，避免只优化其中一个。
- **Micro-F1**：把所有样本的 TP/FP/FN 先加起来再算，更容易被大类别影响。
- **Macro-F1**：每个类别分别算 F1 再平均，更能体现小类别表现。
- **Pass rate / 通过率**：满足某个合格标准的样本占比。
- **Severity-weighted score / 严重度加权分**：严重医疗错误比轻微措辞问题扣更多分。

## 4.3 医疗评测维度

- **Factuality / 事实性**：回答里的医学事实是否正确。
- **Diagnostic rationality / 诊疗合理性**：诊断和下一步处理是否符合临床逻辑，而不是只看最终疾病名称对不对。
- **Guideline consistency / 指南一致性**：回答是否符合可信且适用版本的临床指南。
- **Medication safety / 用药安全**：剂量、禁忌、相互作用和特殊人群建议是否安全。
- **Safety risk / 安全风险**：回答是否可能导致严重健康伤害。
- **Uncertainty / 不确定性表达**：证据不足时模型是否知道自己不确定，而不是硬编结论。
- **Abstention / 拒答或保留判断**：模型在不应该强行回答时主动说“信息不足/需要医生判断”。
- **Calibration / 校准**：模型说自己 80% 有把握时，长期来看是否真的大约 80% 正确。
- **High-risk slice / 高风险切片**：即使整体分数高，也必须单独观察可能造成严重伤害的那类病例。

## 4.4 LLM-as-Judge

- **LLM-as-Judge**：让另一个大模型按照 rubric 给被测模型答案评分。
- **Judge calibration / Judge 校准**：先拿专家标注样本检查这个 Judge 是否真的会按我们想要的标准打分。
- **Human validation / 人工验证**：让真实专家检查自动评分是否靠谱。
- **Expert adjudication / 专家裁决**：自动评测或多个标注者意见冲突时，由专家做最终判断。
- **Agreement / 一致性**：两个 judge 或 judge 与专家给出的判断有多一致。
- **Inter-judge disagreement / Judge 分歧**：多个评分者对同一样本差异很大，说明样本或 rubric 可能有问题。
- **Leniency bias / 宽松偏差**：Judge 天生喜欢给高分。
- **Position bias / 位置偏差**：Judge 会因为答案 A/B 出现顺序而偏向某一边。
- **Verbosity bias / 长度偏差**：Judge 因为答案写得长就错误地觉得它更好。
- **Judge drift / Judge 漂移**：Judge 模型或 prompt 更新后，打分尺度悄悄改变。
- **Circular evaluation / 循环评测**：真值和评分都依赖同一个模型体系，导致看似高分但没有独立证据。

## 4.5 Benchmark 可靠性

- **Contamination / 数据污染**：测试题或高度相似内容曾经出现在模型训练数据或检索库里。
- **Leakage / 泄露**：模型意外获得了本来不应该看到的答案相关信息。
- **Shortcut / 捷径**：模型没有学到目标能力，却利用题目长度、格式、标签分布等无关线索拿到高分。
- **Template leakage / 模板泄露**：题目格式本身暴露了答案规律。
- **Metadata shortcut / 元数据捷径**：模型通过 ID、文件名、类别字段等非目标信息猜答案。
- **Naive baseline / 简单基线**：故意用一个很简单的方法做题，看 benchmark 是否连简单捷径都挡不住。
- **Oracle / 理想方法**：合法使用目标信息和正确方法时理论上应该能很好完成任务的参考系统。
- **Oracle–naive separation**：好 benchmark 应该让真正使用目标能力的方法明显优于简单投机方法。
- **Counterfactual / 反事实测试**：只改一个不应影响答案的因素，看模型是否仍然给出一致合理判断。
- **Ablation / 消融实验**：故意去掉某个模块或信息，看性能下降多少，从而判断它到底有没有作用。
- **Held-out / 留出集**：设计时没有用来调规则或调模型的一部分数据，只在最后验证泛化能力。
- **Paired comparison / 配对比较**：让模型 A/B 在完全相同的样本上比较，减少样本差异带来的噪音。
- **Confidence interval / 置信区间**：告诉你一个测出来的分数大概有多大统计不确定性，而不是把 82.1 当成绝对精确。

## 4.6 RAG / Agent / Tool Calling

- **Prompt**：你发给大模型的指令和上下文。
- **Prompt engineering**：通过改写指令、示例和上下文，让模型更稳定地完成任务。
- **RAG / Retrieval-Augmented Generation**：先从外部知识库找相关资料，再让模型基于这些资料回答。
- **Retrieval / 检索**：从大量文档中找到与当前问题最相关的内容。
- **Chunking / 分块**：把长文档切成适合检索的小段。
- **Embedding / 向量表示**：把文本或图片转换成一串数字，让机器可以计算“像不像/相关不相关”。
- **Vector database / 向量数据库**：专门存 embedding 并快速找相似内容的数据库。
- **Reranking / 重排序**：检索出一批候选后，再用更强的方法重新排列最相关结果。
- **Retrieval recall / 检索召回率**：真正需要的证据有多少被检索系统找到了。
- **Citation faithfulness / 引用忠实性**：回答声称来自某个文档的内容是否真的被那个文档支持。
- **Hallucination after retrieval / 检索后幻觉**：资料已经给对了，但模型仍然编造资料里没有的内容。
- **Agent / 智能体**：模型不只回答文字，还能规划步骤、调用工具、读取结果再继续完成任务。
- **Tool calling / 工具调用**：模型按照固定格式请求搜索、计算器、数据库或其他程序执行操作。
- **Function calling**：Tool calling 的常见形式，模型输出函数名和参数，由系统真正执行函数。
- **Tool schema / 工具参数规范**：规定一个工具有哪些参数、每个参数是什么类型。
- **Trajectory / Agent 轨迹**：Agent 从开始到结束经历的全部思考外部动作、工具调用和观察结果序列。
- **Observation / 工具观察结果**：工具执行完成后返回给 Agent 的信息。
- **Agent evaluation / Agent 评测**：不仅看最终答案，还看工具是否选对、参数是否正确、步骤是否合理。
- **Offline eval / 离线评测**：上线前在固定数据集上测试系统。
- **Online metric / 在线指标**：真实用户使用产品后产生的点击、满意度、成功率等指标。

## 4.7 数据建设

- **Data collection / 数据采集**：从指南、论文、药品说明书、数据库等来源收集原始资料。
- **Annotation / 标注**：人工或程序给数据添加标签、标准答案或评分。
- **QC / Quality Control / 质控**：检查数据有没有错误、重复、缺字段、低质量或不一致。
- **Source authority / 来源权威性**：判断信息来源是否足够可信，例如正式指南通常比无来源帖子更可靠。
- **Temporal validity / 时效性**：知识在当前时间是否仍然有效，旧版指南可能已经过期。
- **Annotation agreement / 标注一致性**：不同标注者对同一样本是否给出相似判断。
- **Synthetic data / 合成数据**：用规则或模型人工生成的新训练/评测样本。
- **Self-training circularity / 自训练循环问题**：模型生成数据、再用同类模型给自己证明这些数据正确，可能不断放大原有错误。
- **PHI / Protected Health Information**：能够识别患者身份的敏感医疗信息。

---

# 5. 大模型最基础概念（A/B 两个岗位都可能出现）

- **LLM / Large Language Model**：通过大量文本训练、能够预测和生成语言的大型神经网络。
- **Transformer**：当前大模型最常见的网络架构，核心机制是 attention。
- **Token**：模型读取文字时使用的基本单位，一个汉字、词或词的一部分都可能成为 token。
- **Tokenizer**：把原始文字切成 token 并转成数字 ID 的工具。
- **Embedding**：把 token、图片等对象变成神经网络可以处理的向量。
- **Hidden state / 隐藏状态**：模型中间层对当前信息形成的一串数字表示。
- **Hidden size**：每个 token 的隐藏向量有多少个数字维度。
- **Layer / 层**：神经网络重复堆叠的一组计算模块。
- **Attention / 注意力**：让一个 token 根据其他 token 的相关程度有选择地读取信息。
- **Self-attention / 自注意力**：同一组 token 之间互相计算相关性并交换信息。
- **Q / Query**：当前 token “想找什么信息”的向量表示。
- **K / Key**：每个 token “我这里有什么信息”的索引向量。
- **V / Value**：真正会被取回来并组合的内容向量。
- **Multi-head attention / 多头注意力**：同时用多组 Q/K/V 从不同角度读取信息。
- **Context window / 上下文窗口**：模型一次最多能读取多少 token。
- **Inference / 推理**：模型训练完成后真正拿输入生成答案的过程。
- **Parameter / 模型参数**：神经网络训练过程中学到的大量数字权重。
- **Loss / 损失函数**：用一个数衡量模型当前预测有多差，训练目标通常是让 loss 下降。
- **Gradient / 梯度**：告诉模型参数应该往哪个方向改才能降低 loss 的信息。
- **Optimizer / 优化器**：根据梯度真正更新模型参数的算法，例如 AdamW。
- **Learning rate / 学习率**：每次参数更新迈多大一步。
- **Epoch**：训练数据完整看过一遍叫一个 epoch。
- **Batch size**：一次同时送进模型训练的样本数量。
- **Overfitting / 过拟合**：模型把训练数据记得很好，但面对新数据表现变差。
- **Generalization / 泛化**：模型面对没见过的新样本仍然能够正确处理。

---

# 6. Track B：CV / ViT / MLLM

## 6.1 计算机视觉基础

- **CV / Computer Vision**：让计算机理解图片、视频等视觉信息的技术领域。
- **Pixel / 像素**：数字图像最基本的小格子，每个像素保存颜色或灰度信息。
- **Image resolution / 图像分辨率**：图片横向 × 纵向有多少像素，例如 224×224。
- **CNN / 卷积神经网络**：传统主流视觉网络，通过局部卷积逐层提取边缘、纹理和高级视觉特征。
- **Convolution / 卷积**：用一个小窗口在图像上滑动，寻找局部模式。
- **Inductive bias / 归纳偏置**：模型结构预先假设“什么规律更可能成立”，例如 CNN 天生偏向局部和平移规律。
- **Feature / 特征**：模型从原始图像中提取出的有用数字表示。
- **Feature map / 特征图**：保留空间位置的视觉特征矩阵。

## 6.2 ViT

- **ViT / Vision Transformer**：把图片切成很多小块，再像处理文字 token 一样用 Transformer 处理这些图像块。
- **Patch / 图像块**：ViT 把一张大图切成的小方块，例如 16×16 像素。
- **Patch embedding**：把每个图像 patch 转成一个固定长度的向量，作为视觉 token。
- **Visual token / 视觉 token**：代表一个图像块或视觉区域的向量，作用类似语言模型里的文字 token。
- **Positional embedding / 位置编码**：告诉模型每个视觉 token 原来在图片哪个位置，否则 patch 打乱顺序模型也不知道。
- **CLS token**：人为添加的一个特殊 token，常用来汇总整张图的信息做分类。
- **Pooling / 池化**：把很多 token 的信息汇总成一个整体表示。
- **ViT pretraining / ViT 预训练**：先用大量图像或图文数据让 ViT 学会通用视觉表示。
- **Fine-tuning / 微调**：拿已经训练好的模型，用更小的目标数据继续训练，让它适应具体任务。

## 6.3 MLLM

- **MLLM / Multimodal Large Language Model**：能同时理解图片和文字，并用语言回答问题的大模型。
- **Vision encoder / 视觉编码器**：把原始图片转换成一串视觉特征或视觉 token 的模型模块。
- **Language model / 语言模型**：接收 token 并生成文字答案的 Transformer 模型。
- **Projector / 投影层**：把视觉编码器输出的维度转换成语言模型能接收的 hidden size。
- **Adapter / 适配器**：连接两个已经训练好的模型模块的小型可训练网络。
- **Q-Former**：用一组可学习 query 从大量视觉特征里挑取和压缩对语言最有用的信息。
- **Resampler**：把很多视觉 token 压缩成更少、更固定数量 token 的模块。
- **LLaVA-style architecture**：典型做法是“视觉编码器 → projector → LLM”，让视觉 token 和文字 token 一起进入语言模型。
- **Multimodal alignment / 多模态对齐**：让模型知道某段文字对应图片里的什么视觉内容。
- **Visual instruction tuning / 视觉指令微调**：用“图片 + 指令 + 答案”数据训练模型按照人类问题理解和描述图片。
- **Detail Caption / 细粒度图像描述**：要求模型尽可能完整、具体地描述图片中的对象、属性、位置和关系。
- **High-resolution / 高分辨率输入**：给模型更清楚的大图以保留小目标和细节，但会显著增加计算量。
- **Tiling / 切块高分辨率**：把大图切成多个小图分别编码，再把这些视觉信息合并。
- **Token length / token 数量**：视觉 token 越多通常保留细节越多，但 attention 计算和显存也更贵。
- **Tensor / 张量**：神经网络中用来存放多维数字数据的结构，可以理解成“多维数组”。
- **Shape / 张量形状**：说明一个张量每个维度有多大，例如 `[batch, tokens, hidden]`。
- **`[B, N, D]`**：常表示 B 个样本、每个样本 N 个 token、每个 token D 维。
- **Grounding / 视觉落地**：模型说到某个对象时，能把它和图片中的真实位置或区域对应起来。
- **Multimodal hallucination / 多模态幻觉**：模型描述了图片里其实不存在的结构、病灶或事实。
- **Image–report alignment / 图像报告对齐**：医学影像中的真实视觉发现与报告文字是否正确对应。

---

# 7. Track B：Pretraining / SFT / LoRA / RL

- **Pretraining / 预训练**：用超大规模数据先让模型学会通用语言、视觉或多模态规律。
- **Pretraining objective / 预训练目标**：预训练时模型具体被要求最小化什么任务的 loss，例如预测下一个 token。
- **Post-training / 后训练**：基础模型预训练完成后，为了让它更会听指令、更安全、更有用而继续训练的阶段。
- **SFT / Supervised Fine-Tuning**：用“输入 → 人类期望答案”的监督数据直接训练模型模仿高质量回答。
- **Instruction tuning / 指令微调**：SFT 的常见形式，用大量不同指令和答案让模型更会遵循用户要求。
- **LoRA**：冻结大部分原模型参数，只训练少量低秩小矩阵，从而低成本微调大模型。
- **PEFT**：Parameter-Efficient Fine-Tuning 的总称，即只训练很少一部分额外参数来适配大模型。
- **Preference data / 偏好数据**：同一个问题给出两个或多个答案，并标出人类更喜欢哪一个。
- **Chosen / Rejected**：偏好训练里分别表示“更好的答案”和“较差的答案”。
- **DPO**：不用单独训练 reward model，直接用 chosen/rejected 对让模型提高好答案相对概率的方法。
- **Reward / 奖励**：用一个数字表示某个模型回答有多符合我们想要的行为。
- **Reward model / 奖励模型**：输入问题和回答后给出 reward 分数的模型。
- **RL / Reinforcement Learning / 强化学习**：模型生成行为后根据 reward 调整策略，让高奖励行为更容易出现。
- **Policy / 策略模型**：强化学习里真正负责生成答案、需要被优化的大模型。
- **Rollout / 采样轨迹**：让当前模型实际生成一个或多个完整回答，供 reward 和 RL 学习使用。
- **PPO**：经典 RLHF 算法，用 reward 更新策略，同时限制新模型不要一次偏离旧模型太远。
- **GRPO**：对同一问题采样一组回答，用组内相对 reward 做学习，从而减少对单独 value model 的依赖。
- **KL divergence / KL 散度**：衡量两个模型输出分布差多远，RL 中常用来限制模型别被 reward 推得太偏。
- **KL regularization / KL 约束**：在 reward 之外额外惩罚模型偏离参考模型太多。
- **Reward hacking / 奖励投机**：模型找到一种“骗分”方式拿到高 reward，但真实质量并没有提升。
- **Alignment / 对齐**：让模型行为更符合人类期望、任务目标和安全要求。
- **Reference model / 参考模型**：RL/DPO 中用来约束当前模型不要偏离过远的基准模型。
- **Value model / 价值模型**：PPO 中估计当前状态未来大概能拿多少 reward 的模型。

---

# 8. 医疗模型训练里常见的特殊问题

- **Medical factuality reward**：奖励模型因为医学事实正确而获得更高分。
- **Guideline consistency reward**：奖励回答符合适用临床指南。
- **Safety reward**：严重危险建议应得到显著负奖励，而不能被语言流畅度抵消。
- **Rare high-risk cases / 稀有高风险样本**：数量不多但一旦答错后果严重的病例，训练和评测时不能被普通样本淹没。
- **Expert preference data / 专家偏好数据**：由医生或领域专家判断多个回答哪个更可靠，而不是完全依赖普通众包标注。
- **Medical hallucination / 医疗幻觉**：模型编造不存在的疾病证据、药物信息、指南内容或影像发现。
- **Overclaim / 过度结论**：现有证据只支持“可能”，模型却说成“确定”。
- **Evidence sufficiency / 证据充分性**：现有信息到底够不够支撑模型给出的结论。

---

# 9. 面试里经常听到但容易懵的词

- **Baseline / 基线**：先用一个简单或当前已有的方法作为比较起点。
- **SOTA / State of the Art**：当前公开结果中表现最好的或非常领先的方法。
- **A/B test**：把两个方案放到相同条件下比较真实效果。
- **Trade-off / 权衡**：一个方案通常同时有好处和代价，需要说明你愿意牺牲什么换取什么。
- **Scalability / 可扩展性**：数据量和请求量大很多以后，系统还能不能正常工作。
- **Throughput / 吞吐量**：单位时间能处理多少任务或请求。
- **Latency / 延迟**：一次请求从开始到得到结果要等多久。
- **Robustness / 鲁棒性**：输入有噪声、缺失或异常时，系统是否仍然稳定。
- **Reproducibility / 可复现性**：别人拿相同数据、版本和代码，能不能重新得到相近结果。
- **Production / 生产环境**：真正给业务和用户使用的系统，而不是本地 Demo。
- **Prototype / 原型**：用来快速验证想法是否可行的早期版本，不要求达到生产级稳定性。
- **Ablation study / 消融实验**：一次去掉一个组件，看它到底贡献了多少。
- **Hyperparameter / 超参数**：训练开始前人为设置、而不是模型自动学出来的参数，例如 learning rate。
- **Inference cost / 推理成本**：模型真正在线回答问题时需要的 GPU 时间、显存或金钱。
- **Training cost / 训练成本**：训练或微调模型所消耗的计算资源和时间。

---

# 10. 你现在不需要立刻搞懂的东西

第一次看到下面这些词时，只需要知道一句话含义，不要求会公式或手写实现：

- PPO 的完整数学推导；
- GRPO 的 loss 公式；
- Attention 的矩阵推导；
- ViT 的完整训练细节；
- Quickselect partition 的全部边界技巧；
- LC25 的链表手写；
- 复杂动态规划；
- 分布式训练底层通信；
- CUDA kernel；
- 复杂统计检验。

当前原则：

> **先能听懂面试官和题目在说什么，再逐步学会回答；不要把“第一次没见过名词”误认为“我没有能力学这个”。**
